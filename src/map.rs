pub mod map {
    use std::path;

    struct Map {
        resolution: u64,
        width: u64,
        height: u64,
    }

    impl Map {
        pub fn readMap(file: Path) {
            todo!();
        }
    }
}
